# Backend base URL (no trailing slash).
# localhost works in iOS Simulator only.
# On a physical device, leave as localhost — the app auto-uses your Metro LAN IP in dev.
# EXPO_PUBLIC_API_URL=https://meal-hisab-api.dresspalli.com
EXPO_PUBLIC_API_URL=http://localhost:8000/api/v1